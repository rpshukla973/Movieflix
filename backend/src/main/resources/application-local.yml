spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/movieflix}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:Sp9411668277@}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

server:
  port: ${SERVER_PORT:9090}

app:
  jwt:
    secret: ${JWT_SECRET:U29tZVN1cGVyU2VjdXJlQmFzZTY0S2V5Rm9yTW92aWVmbGl4MTIzNDU2Nzg5MA==}
    access-token-ttl-ms: ${JWT_ACCESS_TOKEN_TTL_MS:900000}
  storage:
    provider: local
    local-path: ./storage
